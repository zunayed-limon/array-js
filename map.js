

// map(): returns a new array containing the results of calling a given function on each element of an array

const myArray = [1, 2, 3];
const doubled = myArray.map(num => num * 2);
console.log(doubled); // outputs [2, 4, 6]
