
// sort(): sorts the elements in an array in place, with an optional comparison function to specify the sort order

const myArray = ['orange', 'apple', 'banana'];
myArray.sort();
console.log(myArray); // output: ['apple', 'banana', 'orange']
